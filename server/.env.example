# JSON mapping: use a unique random 32+ character token per user; never put tokens in frontend builds.
TRAVEL_API_TOKENS={"replace-with-a-random-token-at-least-32-characters":"owner-1"}
TENCENT_MAP_KEY=
# Required when signature verification is enabled; server only.
TENCENT_MAP_SK=
DATA_DIR=/data
HOST=0.0.0.0
PORT=8787
ALLOWED_ORIGIN=
