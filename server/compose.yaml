services:
  travel:
    build:
      context: ..
      dockerfile: server/Dockerfile
    env_file: .env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - travel-data:/data
    restart: unless-stopped
volumes:
  travel-data:
