export class ApiError extends Error{status:number;current:any;constructor(message:string,status:number,current:any){super(message);this.status=status;this.current=current}}
export async function api<T=any>(path:string,method='GET',body?:unknown):Promise<T>{const r=await fetch('/api'+path,{method,credentials:'same-origin',headers:body?{'Content-Type':'application/json'}:undefined,body:body?JSON.stringify(body):undefined});let value:any;try{value=await r.json()}catch{throw new ApiError('无法读取服务响应',r.status,null)}if(!r.ok)throw new ApiError(value.message||'请求失败',r.status,value.current);return value}
